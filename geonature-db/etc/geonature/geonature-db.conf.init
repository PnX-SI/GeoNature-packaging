POSTGRES_HOST="${POSTGRES_HOST}"
POSTGRES_PORT="${POSTGRES_PORT}"
POSTGRES_USER="${POSTGRES_USER}"
POSTGRES_PASSWORD="${POSTGRES_PASSWORD}"
POSTGRES_DB="${POSTGRES_DB}"
POSTGRES_LOCALE="${POSTGRES_LOCALE}"
NOMENCLATURE_LANGUAGE="${NOMENCLATURE_LANGUAGE}"
LOCAL_SRID="${LOCAL_SRID}"
ADD_USERSHUB_SAMPLE_DATA="${ADD_USERSHUB_SAMPLE_DATA}"
REFGEO_MUNICIPALITY="${REFGEO_MUNICIPALITY}"
REFGEO_GRID="${REFGEO_GRID}"
REFGEO_DEM="${REFGEO_DEM}"
REFGEOREFGEO_VECTORISE_DEM_DEM="${REFGEO_VECTORISE_DEM}"
